---
name: lark-shared
description: Built-in lark-shared workflow
---

Use the group project configuration supplied with this task. Never infer role from profile name or call legacy project binding scripts. Report missing configuration explicitly. Before using lark-cli run the relevant command --help. Use explicit identity and inspect the ok field. Never disclose credentials. Read before writing; write only within the user request. Never send to other chats without explicit authorization.
